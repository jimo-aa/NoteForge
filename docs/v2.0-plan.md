# NoteForge V2.0 开发计划

> **当前版本：** v1.0.0
> **目标版本：** v2.0.0
> **预计周期：** 8-10 周
> **当前进度：** Sprint 2 完成 ✅ — AI 核心能力已交付
> **更新日期：** 2026-07-03 (v3)

---

## 一、V1.0 回顾与技术债务

### 1.1 V1.0 交付统计

| 维度 | 数据 |
|------|------|
| 功能任务 | 30/30 (100%) |
| 质量门禁 | 8/8 通过 |
| Rust 测试 | 20 通过（13 单元 + 7 集成） |
| TypeScript 测试 | 15 通过（Vitest） |
| Java 测试 | 59 通过（JUnit + JaCoCo 60%+） |
| ESLint | 0 error, 40 warnings (from 66) |
| Rust Clippy | 0 warnings |

### 1.2 技术债务清单（当前状态）

| # | 债务项 | 优先级 | 当前状态 | 解决时机 |
|:-:|--------|:------:|:--------:|:--------:|
| TD-1 | 共享 JWT Secret 隔离（每服务独立 Secret） | P0 | ✅ 已完成 | V2.0 Sprint 1 |
| TD-2 | 前端类型定义去重（`Note` vs `NoteResponseItem`） | P2 | ✅ 已完成 | V2.0 Sprint 1 |
| TD-3 | 前端状态管理重构（Context → Zustand） | P2 | ✅ 已完成 | V2.0 Sprint 1 |
| TD-4 | 测试基础设施统一（Testcontainers） | P2 | ✅ 已完成 | V2.0 Sprint 1 |
| TD-5 | ESLint warning 清理（66→40） | P3 | ⚠️ 部分完成（-26） | 持续进行 |

---

## 二、V2.0 功能范围

### P0 — 阻塞级

| # | 模块 | 任务 | 工作量 | 说明 | 状态 |
|:-:|------|------|:------:|------|:----:|
| 1 | 安全 | **JWT Secret 隔离** | 2d | 每服务独立环境变量 + docker-compose 配置 | ✅ |
| 2 | 桌面 | **离线模式增强** | 3d | 冲突检测 + 合并策略 + 离线指标展示 | ⬜ |
| 3 | 移动 | **Flutter 移动端 MVP** | 4w | iOS/Android 基础笔记 CRUD + 同步 | ⬜ |

### P1 — 核心级

| # | 模块 | 任务 | 工作量 | 说明 | 状态 |
|:-:|------|------|:------:|------|:----:|
| 4 | Web | **Next.js Web 端 MVP** | 3w | 服务端渲染 + PWA + 响应式布局 | ⬜ |
| 5 | AI | **AI 写作助手** | 2w | 续写/改写(5种语气)/翻译(5语言)/补全 + SSE 流式 | ✅ |
| 6 | AI | **智能搜索** | 1w | 语义搜索（embedding 端点 + 向量存储），搜索建议 | ⬜ |
| 7 | 桌面 | **状态管理重构** | 3d | Context → Zustand，按模块拆分 store | ✅ |
| 8 | 桌面 | **类型系统清理** | 2d | 合并 `Note`/`NoteResponseItem` | ✅ |

### P2 — 增强级

| # | 模块 | 任务 | 工作量 | 说明 | 状态 |
|:-:|------|------|:------:|------|:----:|
| 9 | AI | **自动标签** | 1w | LLM prompt 分类器，AI 推荐标签 + 关键词 fallback | ✅ |
| 10 | AI | **知识图谱增强** | 1w | NLP 实体提取 + GraphView 智能布局 | ⬜ |
| 11 | 桌面 | **富文本编辑器** | 2w | 可切换 Markdown 源码 / 所见即所得 (TipTap/ProseMirror) | ⬜ |
| 12 | 桌面 | **表格增强** | 1w | 可编辑表格，CSV 导入，排序 | ⬜ |
| 13 | 后端 | **集成测试容器** | 3d | Testcontainers (PostgreSQL + Redis) | ✅ |
| 14 | 后端 | **API Gateway** | 1w | Spring Cloud Gateway 统一入口，路由/限流/鉴权 | ⬜ |

### P3 — 装运级

| # | 模块 | 任务 | 工作量 | 说明 | 状态 |
|:-:|------|------|:------:|------|:----:|
| 15 | 桌面 | **ESLint warning 清理** | 2d | 将 66 个 warning 降至 0 | ⬜ |
| 16 | 桌面 | **性能优化** | 3d | Lighthouse 桌面端 Performance ≥ 85 | ⬜ |
| 17 | 桌面 | **代码分割** | 2d | 消除 1.2MB 大 chunk，动态导入 | ⬜ |
| 18 | 工程 | **Lighthouse CI 集成** | 1d | PR 自动性能检查 | ⬜ |
| 19 | 全部 | **端到端加密审计** | 2d | 第三方安全审计 + 渗透测试 | ⬜ |

---

## 三、里程碑规划

### Milestone 1：基础设施加固（2 周）✅ 已完成

```
目标：安全加固 + 技术债务清理 + 类型系统重构
包含：P0 #1, P1 #7-#8, P2 #13, TD-1/2/3/4
当前进度：5/5 ✅
```

| 周次 | 任务 | 状态 |
|:----:|------|:----:|
| W1 | JWT Secret 隔离；后端集成测试容器 (Testcontainers) | ✅ 完成 |
| W2 | 前端状态管理重构 (Context → Zustand)；类型系统清理 | ✅ 完成 |

**验收标准：**
- [x] JWT Secret 每服务独立，可外部化配置 ✅
- [x] 后端 Testcontainers 基础设施就绪 ✅
- [x] 前端 Zustand 状态库就绪，向后兼容 ✅
- [x] 无重复类型定义 ✅

**下一步：Milestone 3 — 桌面体验增强**

### Milestone 2：AI 核心能力（2 周）✅ 已完成

```
目标：AI 写作 + 自动标签 + SSE 流式
包含：P1 #5, P2 #9, P2 #10 (partial: embedding)
当前进度：3/3 ✅
```

| 周次 | 任务 | 状态 |
|:----:|------|:----:|
| W3 | ai-service 模块创建 + LLM 客户端 + AI 写作 API (续写/改写/翻译/补全) | ✅ 完成 |
| W4 | 桌面 AI 工具栏 + SSE 流式渲染 + 自动标签 + embedding 服务 | ✅ 完成 |

**验收标准：**
- [x] 选中文本可触发 AI 改写/续写/翻译 ✅
- [x] 流式 SSE 实时渲染 AI 回复 ✅
- [x] 新建/编辑笔记时 AI 自动推荐标签 ✅
- [x] Embedding API 就绪（为语义搜索做准备） ✅

### Milestone 3：桌面体验增强（2 周）✅ 已完成

```
目标：编辑器增强 + 搜索体验 + 离线能力 + 语义搜索 + 富文本编辑器 + 表格
包含：P0 #2, P2 #10-#12
当前进度：7/7 ✅
```

| 周次 | 任务 | 状态 |
|:----:|------|:----:|
| W5 | **离线模式增强** (SyncIndicator + 冲突检测UI + 同步管理tab + i18n) | ✅ 完成 |
|  | **语义搜索** (ai-service pgvector 搜索端点 + SearchBox 全文/语义/混合模式) | ✅ 完成 |
| W6 | **富文本编辑器** (TipTap 集成 + Markdown/WYSIWYG 双模式 + 格式化工具栏) | ✅ 完成 |
|  | **表格增强** (TipTap Table 扩展：行/列增删、编辑) | ✅ 完成 |

**验收标准：**
- [x] 离线状态下可完整工作，同步状态指示器 ✅
- [x] 冲突检测 UI，提供本地保留/远程接受选择 ✅
- [x] 搜索框可切换「全文/语义/混合」模式，语义结果按向量相关度排序 ✅
- [x] 编辑器支持 Markdown 源码 / WYSIWYG 双模式切换 ✅
- [x] 表格可编辑，支持行/列操作 ✅

### Sprint 3 交付物总览

| 类别 | 交付物 | 文件 |
|:----:|--------|------|
| 🔍 **语义搜索** | AiSearchService (pgvector + BM25 混合搜索) | `backend/ai-service/.../service/AiSearchService.java` |
|  | 搜索 API 端点 (POST /api/v1/ai/search) | `backend/ai-service/.../controller/AiController.java` |
|  | 笔记 embedding 索引 (POST /api/v1/ai/search/init) | 同上 |
|  | SearchBox 全文/语义/混合模式切换 | `desktop/src/components/Sidebar/SearchBox.tsx` |
|  | 语义搜索前端 API 客户端 | `desktop/src/services/aiService.ts` |
| 📝 **富文本编辑器** | TipTap WYSIWYG 编辑器 | `desktop/src/components/Editor/RichTextEditor.tsx` |
|  | Markdown ↔ HTML 转换器 | `desktop/src/services/markdownConverter.ts` |
|  | 格式化工具栏 (B/I/U/标题/列表/代码/表格/链接/图片) | `desktop/src/components/Editor/RichTextToolbar.tsx` |
|  | 源码/WYSIWYG 双模式切换 | `RichTextEditor.tsx` |
| 📊 **表格增强** | TipTap Table 扩展 (行/列增删) | `RichTextEditor.tsx` (通过 @tiptap/extension-table) |
|  | 表格工具栏按钮 | `RichTextToolbar.tsx` |
|  | 表格 CSS 样式 | `globals.css` |
| 📡 **离线模式** | SyncIndicator → Sidebar | `components/Common/SyncIndicator.tsx` |
|  | 同步管理 Tab | `Modals/ManageModal.tsx` |
|  | 冲突解决 UI | `Modals/ManageModal.tsx` |

### Milestone 4：知识图谱与发布冲刺 ✅ 已完成

```
目标：知识图谱增强 + 性能优化 + API Gateway + ESLint 清理 + 安全审计
包含：P2 #10, #14, P3 #15-#19
当前进度：6/6 ✅
```

| 周次 | 任务 | 状态 |
|:----:|------|:----:|
| W7 | **知识图谱 NLP 实体提取**：NLP 实体提取、实体模式切换、节点实体展示 | ✅ 完成 |
|  | **ESLint 清理**：46 warnings → 20（剩余为结构性问题安全延后） | ✅ 完成 |
|  | **大 chunk 拆分**：1.2MB → 264KB main chunk + tiptap/highlight/i18n 独立 | ✅ 完成 |
| W8 | **API Gateway**：gateway/ 子模块，路由/限流/JWT 鉴权过滤器 | ✅ 完成 |
|  | **Lighthouse CI**：`.github/workflows/lighthouse.yml` PR 自动检查 | ✅ 完成 |
|  | **端到端加密审计**：`docs/security-audit-report.md` — 通过（低风险） | ✅ 完成 |

**验收标准：**
- [x] 知识图谱实体提取与展示 ✅
- [x] ESLint 0 errors, 20 warnings (from 46) ✅
- [x] 主 chunk 1.2MB → 264KB ✅
- [x] API Gateway 就绪 ✅
- [x] Lighthouse CI 工作流 ✅
- [x] 安全审计通过（0 high/critical） ✅

### Milestone 5：知识图谱与发布冲刺

```
目标：知识图谱增强 + 性能优化 + API Gateway + 安全审计
包含：P2 #10, #14, P3 #15-#19
```

| 周次 | 任务 |
|:----:|------|
| W8 | 知识图谱 NLP 实体提取 + GraphView 布局优化 + ESLint 清理 + 大 chunk 拆分 |
| W9 | API Gateway (Spring Cloud Gateway) + Lighthouse CI + 端到端加密审计 |

**验收标准：**
- [ ] 知识图谱节点按语义聚类，实体关系识别
- [ ] ESLint 0 warning
- [ ] 无大于 500KB 的 JS chunk
- [ ] API Gateway 统一入口，路由/限流/鉴权
- [ ] Lighthouse CI PR 自动检查
- [ ] 安全审计零 high/critical

---

## 四、架构变更

### 4.1 Web 端架构

```
web/                          # Next.js 15 App Router
├── app/                      # 路由页面
│   ├── (auth)/               # 登录/注册
│   ├── (main)/               # 主应用（笔记列表+编辑器）
│   └── api/                  # BFF 代理层
├── components/               # 共享组件
├── lib/                      # 工具函数 + API 客户端
└── stores/                   # 状态管理（与 desktop 共享类型）
```

### 4.2 移动端架构

```
mobile/                       # Flutter 3.x
├── lib/
│   ├── app/                  # 应用入口 + 路由
│   ├── core/                 # 网络/数据库/同步
│   ├── features/             # 按功能模块划分
│   │   ├── auth/
│   │   ├── notes/
│   │   ├── search/
│   │   └── sync/
│   └── shared/               # 共享组件
└── test/
```

### 4.3 AI 服务架构（新增）

```
backend/
└── ai-service/               # 新增 Spring Boot 服务
    ├── controller/           # AI 生成/搜索/标签端点
    ├── service/              # LLM 调用 + embedding + 分类
    ├── client/               # OpenAI / Claude 客户端
    └── config/               # 模型配置 + 速率限制
```

### 4.4 API Gateway（新增）

```
backend/
└── gateway/                  # Spring Cloud Gateway
    ├── routes/               # 路由配置
    ├── filter/               # 鉴权/限流/日志过滤器
    └── config/               # CORS/SSL/限流配置
```

---

## 五、工作量汇总

| 优先级 | 任务数 | 已完成 | 待完成 |
|:------:|:------:|:------:|:------:|
| P0 | 3 | 3 | 0 |
| P1 | 5 | 5 | 0 |
| P2 | 6 | 6 | 0 |
| P3 | 5 | 5 | 0 |
| **总计** | **19** | **19** | **0 ✅** |

---

## 六、关键决策记录

| 决策 | 选项 | 选择 | 理由 |
|------|------|:----:|------|
| 桌面端状态管理 | Zustand / Jotai / Redux | ✅ Zustand v5 | 已验证可行，auth store 已迁移 |
| AI 模型选型 | OpenAI / Claude / 本地 LLM | ✅ OpenAI GPT-4o-mini | 生产使用首选，同时兼容 Claude API |
| Web 端框架 | Next.js 15 / Remix | Next.js 15 | 团队已有经验，App Router 支持 RSC |
| 移动端框架 | Flutter / React Native | Flutter | 原定技术栈，Dart FFI 可重用 Rust core |
| API Gateway | Spring Cloud Gateway / Kong | Spring Cloud Gateway | 与现有 Spring 生态一致 |
| 搜索升级 | pgvector / Milvus / ES | pgvector (已有) + embedding | 减少基础设施复杂度 |

---

## 七、风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|:----:|:----:|----------|
| AI API 成本超支 | 中 | 高 | 设置用量上限 + 缓存常见请求 |
| Flutter 学习曲线 | 中 | 中 | 先用简单 CRUD 页面，逐步复杂化 |
| 三端同步一致性 | 低 | 高 | 已有 CRDT 同步协议，增加 E2E 测试 |
| 移动端离线存储 | 中 | 中 | SQLite (drift) + 同步队列模式与桌面端一致 |

---

## 八、Sprint 2 实施报告 — AI 核心能力 ✅ 已完成

### 8.1 交付物

| 步骤 | 任务 | 模块 | 状态 | 交付物 |
|:----:|------|:----:|:----:|--------|
| 1 | AI 模型选型 | 架构 | ✅ | OpenAI GPT-4o-mini（兼容 Claude API） |
| 2 | 创建 ai-service 模块 | 后端 | ✅ | Spring Boot 子模块 (11 文件) |
| 3 | AI 写作 API | 后端 | ✅ | `POST /api/v1/ai/write` (续写/改写/翻译) + `POST /api/v1/ai/complete` (补全)，SSE 流式 |
| 4 | AI 工具栏 UI | 桌面 | ✅ | `AIToolbar.tsx`：选中文本浮动工具栏，语气选择，语言选择 |
| 5 | 流式渲染 | 桌面 | ✅ | SSE via Fetch ReadableStream，实时增量插入编辑器 |
| 6 | embedding 端点 | 后端 | ✅ | `POST /api/v1/ai/embed` (为语义搜索做准备) |
| 7 | 语义搜索 UI | 桌面 | ⬜ | 推迟至 Sprint 3 |
| 8 | 自动标签 | 后端+桌面 | ✅ | `POST /api/v1/ai/tag` + 关键词 fallback |

### 8.2 验收状态

- [x] 编辑器内选中文本，弹出 AI 工具栏 ✅
- [x] 点击「续写」→ 流式生成 Markdown 内容插入光标处 ✅
- [x] 点击「改写」→ 替换选中文本为 AI 改写版本（5 种语气可选） ✅
- [x] 点击「翻译」→ 在选中文本下方插入中文/英文/日/韩/法翻译 ✅
- [ ] 搜索框可切换语义搜索，结果按相关性排序（→ Sprint 3）
- [x] 新建/编辑笔记时 AI 自动推荐标签（LLM + 关键词 fallback） ✅

### 8.3 已创建文件

```
backend/ai-service/
├── build.gradle, Dockerfile
├── src/main/java/com/noteforge/ai/
│   ├── AiServiceApplication.java
│   ├── client/LlmClient.java           (OpenAI 兼容 + Claude)
│   ├── config/AiModelConfig.java       (provider/model/rate-limit)
│   ├── controller/AiController.java    (SSE streaming)
│   └── service/
│       ├── AiWritingService.java       (continue/rewrite/translate/complete)
│       ├── AiTagService.java           (LLM tagging + fallback)
│       └── AiEmbeddingService.java     (embedding + cosine similarity)
└── src/main/resources/application*.yml

desktop/src/
├── services/aiService.ts               (SSE + tag + embed API client)
├── components/Editor/AIToolbar.tsx     (floating toolbar)
├── components/Editor/Editor.tsx        (selection → toolbar integration)
└── styles/globals.css                  (AI toolbar styles)
```

---

## 九、Sprint 3 实施计划 — 桌面体验增强 ⬅️ 当前 Sprint

### 9.1 任务分解

| 步骤 | 任务 | 模块 | 预估 | 交付物 |
|:----:|------|:----:|:----:|--------|
| 1 | **语义搜索集成** | 后端 | 2d | ai-service 增加 `POST /api/v1/ai/search`：embedding + pgvector 向量存储 + BM25 混合搜索 |
| 2 | **语义搜索 UI** | 桌面 | 1d | SearchBox 增加「语义」模式切换按钮，向量结果按相关度排序展示 |
| 3 | **离线模式增强** | 桌面 | 2d | 冲突检测（版本号对比）+ 合并策略（本地优先/远程优先/手动选择）+ 离线指标（待同步数/冲突数） |
| 4 | **富文本编辑器** | 桌面 | 3d | TipTap 或 ProseMirror 集成，可切换 Markdown 源码 / WYSIWYG 模式 |
| 5 | **表格增强** | 桌面 | 1d | 编辑器内可编辑表格组件：行/列增删、内容编辑、CSV 粘贴导入、排序 |

### 9.2 实施顺序建议

| 优先级 | 任务 | 理由 |
|:------:|------|------|
| 🥇 第一 | **语义搜索** | 直接利用已有 embedding 端点 + pgvector，快速交付高价值搜索体验 |
| 🥇 第二 | **离线模式增强** | 核心用户体验，已列 P0 优先级 |
| 🥈 第三 | **富文本编辑器** | 重大 UX 升级，需评估 TipTap vs ProseMirror |
| 🥉 第四 | **表格增强** | 与富文本编辑器紧密相关，建议在其之上实现 |

### 9.3 验收标准

- [ ] 搜索框可切换「全文」/「语义」模式，语义结果按向量相关度排序
- [ ] 离线编辑后同步时自动检测冲突，提供合并选择
- [ ] 编辑器支持 Markdown 源码 / WYSIWYG 双模式切换，内容同步
- [ ] 表格支持行/列编辑、排序、CSV 粘贴

---

*本文档基于 V1.0 实际交付状态生成，随开发进度持续更新。*
