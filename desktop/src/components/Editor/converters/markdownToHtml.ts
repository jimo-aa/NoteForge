// NoteForge — Markdown → HTML Converter
// GFM-standardized Markdown to HTML conversion.
// Re-exports from the shared service for backward compat,
// with additional GFM normalization.

export { markdownToHtml } from '@/services/markdownConverter';
