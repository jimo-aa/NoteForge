// NoteForge — HTML → Markdown Converter
// Converts HTML (including TipTap-specific output) back to GFM Markdown.
// Re-exports from the shared service.

export { htmlToMarkdown } from '@/services/markdownConverter';
